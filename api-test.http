### ============================================
### hr-security 接口测试文件（IDEA 内置 HTTP Client）
### 用法：确保应用已启动，然后逐个点左侧绿色箭头运行
### ============================================

### 1. 注册（密码以 BCrypt 密文落库）
POST http://localhost:8080/api/auth/register
Content-Type: application/json

{
  "username": "admin",
  "password": "123456",
  "nickname": "管理员"
}

### 2. 登录成功，自动把返回的 token 存为全局变量，供下面接口使用
POST http://localhost:8080/api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "123456"
}

> {%
    client.global.set("token", response.body.data.token);
%}

### 3. 获取当前用户（带 token 才能访问，无 token 返回 401）
GET http://localhost:8080/api/auth/me
Authorization: Bearer {{token}}

### 4. 故意不带 token 访问（应该返回 401 JSON）
GET http://localhost:8080/api/auth/me

### 5. 密码错误登录（应该返回 400 用户名或密码错误）
POST http://localhost:8080/api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "wrong-password"
}
